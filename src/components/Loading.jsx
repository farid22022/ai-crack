

const Loading = () => {
    return (
        <div className="text-center ">
            <span className="loading loading-spinner text-error w-full"></span>
        </div>
    );
};

export default Loading;